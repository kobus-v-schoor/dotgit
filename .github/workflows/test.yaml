name: test
on: push

jobs:
  test:
    runs-on: '${{ matrix.os}}'

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: run tests
        run: ./test.py
        working-directory: tests
